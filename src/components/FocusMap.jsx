const domains = ["Network", "Web", "IoT", "Identity", "Assessment", "Defense"];
export default function FocusMap() { return <div className="focus-map"><div className="radar" aria-hidden="true">{[1,2,3].map(ring => <i key={ring} className={`radar-ring ring-${ring}`} />)}{domains.map((domain, index) => <span key={domain} style={{ "--angle": `${index * 60}deg` }}>{domain}</span>)}<b>SECURITY<br />FOCUS</b></div><div className="focus-copy"><p className="section-label">SECURITY FOCUS MAP</p><h3>Practical learning,<br /><em>layered defense.</em></h3><p>A compact view of the security domains explored through coursework, labs, and project work.</p><div>{domains.map(domain => <span key={domain}>{domain}</span>)}</div></div></div>; }
