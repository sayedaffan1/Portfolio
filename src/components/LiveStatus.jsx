import { useEffect, useState } from "react";
export default function LiveStatus() { const [time, setTime] = useState(""); useEffect(() => { const tick = () => setTime(new Intl.DateTimeFormat("en-GB", { timeZone:"Asia/Kolkata", hour:"2-digit", minute:"2-digit", second:"2-digit", hour12:false }).format(new Date())); tick(); const id = setInterval(tick, 1000); return () => clearInterval(id); }, []); return <div className="live-status"><span><i /> SYSTEM ONLINE</span><span>IST {time}</span></div>; }
