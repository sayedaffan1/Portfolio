import { useEffect, useState } from "react";
const lines = ["affan@portfolio:~$ whoami", "Affan Sayed", "Cybersecurity Enthusiast", "", "affan@portfolio:~$ status", "[✓] Open to opportunities", "[✓] Cybersecurity Projects", "[✓] Continuous Learning"];
export default function Terminal() { const [shown, setShown] = useState(0); useEffect(() => { const id = setInterval(() => setShown(n => n < lines.length ? n + 1 : n), 360); return () => clearInterval(id); }, []); return <div className="terminal-window" role="region" aria-label="Portfolio terminal"><div className="terminal-bar"><span /><span /><span /><p>affan@portfolio — bash</p></div><div className="terminal-body">{lines.slice(0, shown).map((line, i) => <p key={i} className={line.startsWith("affan") ? "command" : ""}>{line || " "}</p>)}{shown < lines.length && <i className="terminal-caret" />}</div></div>; }
