import { useMemo } from "react";
const glyphs = "01AF<>/{}#%*+";
export default function MatrixRain() { const columns = useMemo(() => Array.from({ length: innerWidth < 800 ? 14 : 34 }, (_, index) => ({ id:index, left:`${(index + Math.random() * .75) * 3}%`, delay:`-${Math.random() * 18}s`, duration:`${9 + Math.random() * 11}s`, opacity:.28 + Math.random() * .42, text:Array.from({ length:22 }, () => glyphs[Math.floor(Math.random() * glyphs.length)]).join("\n") })), []); return <div className="matrix-rain" aria-hidden="true">{columns.map(column => <span key={column.id} style={{ left:column.left, animationDelay:column.delay, animationDuration:column.duration, opacity:column.opacity }}>{column.text}</span>)}</div>; }
