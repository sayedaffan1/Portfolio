import { useMemo } from "react";
const glyphs = "01AF<>/{}#%*+";
export default function MatrixRain() { const columns = useMemo(() => { const count = innerWidth < 800 ? 9 : 34; return Array.from({ length:count }, (_, index) => ({ id:index, left:`${(index / (count - 1)) * 100 + (Math.random() * 2 - 1)}%`, delay:`-${Math.random() * 18}s`, duration:`${innerWidth < 800 ? 14 : 9 + Math.random() * 11}s`, opacity:innerWidth < 800 ? .22 + Math.random() * .14 : .28 + Math.random() * .42, text:Array.from({ length:innerWidth < 800 ? 15 : 22 }, () => glyphs[Math.floor(Math.random() * glyphs.length)]).join("\n") })); }, []); return <div className="matrix-rain" aria-hidden="true">{columns.map(column => <span key={column.id} style={{ left:column.left, animationDelay:column.delay, animationDuration:column.duration, opacity:column.opacity }}>{column.text}</span>)}</div>; }
