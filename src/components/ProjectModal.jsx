import { useEffect } from "react";
import { AnimatePresence, motion } from "framer-motion";
export default function ProjectModal({ project, onClose }) { useEffect(() => { const close = e => e.key === "Escape" && onClose(); if (project) { document.addEventListener("keydown", close); document.body.style.overflow = "hidden"; } return () => { document.removeEventListener("keydown", close); document.body.style.overflow = ""; }; }, [project, onClose]); return <AnimatePresence>{project && <motion.div className="modal-backdrop" onMouseDown={onClose} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.article className="project-modal" onMouseDown={e => e.stopPropagation()} initial={{ opacity: 0, y: 28 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 28 }}><button className="modal-close" onClick={onClose} aria-label="Close project details">×</button><span className="modal-num">PROJECT / {project.num}</span><h2>{project.title}</h2><p className="modal-detail">{project.detail}</p><div className="modal-grid"><div><h3>Technologies</h3><div className="tag-list">{project.technologies.map(x => <span key={x}>{x}</span>)}</div></div><div><h3>Security concepts</h3><div className="tag-list">{project.concepts.map(x => <span key={x}>{x}</span>)}</div></div></div><div className="modal-objective"><h3>Objective</h3><p>{project.objective}</p></div></motion.article></motion.div>}</AnimatePresence>; }
