let context;
let ambientNodes = [];
function audioContext() { context ||= new (window.AudioContext || window.webkitAudioContext)(); return context; }
function tone(frequency, start, duration, volume = .055, type = "sine") { const ctx = audioContext(), oscillator = ctx.createOscillator(), gain = ctx.createGain(); oscillator.type = type; oscillator.frequency.setValueAtTime(frequency, start); gain.gain.setValueAtTime(.0001, start); gain.gain.exponentialRampToValueAtTime(volume, start + .012); gain.gain.exponentialRampToValueAtTime(.0001, start + duration); oscillator.connect(gain).connect(ctx.destination); oscillator.start(start); oscillator.stop(start + duration + .02); }
export function playAccessSound() { const ctx = audioContext(); ctx.resume(); const start = ctx.currentTime + .02; tone(220, start, .09, .045, "square"); tone(365, start + .11, .09, .045, "square"); tone(610, start + .22, .18, .055, "sine"); }
export function playUiSound(primary = false) { const ctx = audioContext(); ctx.resume(); const start = ctx.currentTime + .01; tone(primary ? 560 : 340, start, .08, .04, primary ? "sine" : "triangle"); }
export function startAmbient() { if (ambientNodes.length) return; const ctx = audioContext(); ctx.resume(); const master = ctx.createGain(); master.gain.value = .018; master.connect(ctx.destination); [110, 164.81, 220].forEach((frequency, index) => { const oscillator = ctx.createOscillator(), gain = ctx.createGain(), lfo = ctx.createOscillator(), lfoGain = ctx.createGain(); oscillator.type = index === 1 ? "triangle" : "sine"; oscillator.frequency.value = frequency; gain.gain.value = index === 1 ? .33 : .2; lfo.frequency.value = .035 + index * .012; lfoGain.gain.value = .11; lfo.connect(lfoGain).connect(gain.gain); oscillator.connect(gain).connect(master); oscillator.start(); lfo.start(); ambientNodes.push(oscillator, lfo, gain, lfoGain, master); }); }
export function stopAmbient() { ambientNodes.forEach(node => { try { if (node.stop) node.stop(); else node.disconnect(); } catch {} }); ambientNodes = []; }
