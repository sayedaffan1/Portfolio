import { useEffect, useState } from "react";
export default function UiSound() { const [enabled, setEnabled] = useState(false); useEffect(() => { if (!enabled) return; const play = event => { if (!event.target.closest("button,a")) return; const ctx = new AudioContext(); const oscillator = ctx.createOscillator(), gain = ctx.createGain(); oscillator.frequency.value = event.target.closest(".primary") ? 540 : 280; gain.gain.setValueAtTime(.025, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(.001, ctx.currentTime + .055); oscillator.connect(gain).connect(ctx.destination); oscillator.start(); oscillator.stop(ctx.currentTime + .06); }; document.addEventListener("pointerdown", play); return () => document.removeEventListener("pointerdown", play); }, [enabled]); return <button className={`sound-toggle ${enabled ? "on" : ""}`} onClick={() => setEnabled(value => !value)} aria-label="Toggle interface sound" aria-pressed={enabled}>{enabled ? "◖ SOUND" : "◌ SOUND"}</button>; }
